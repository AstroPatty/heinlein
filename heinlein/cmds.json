{
    "add" : {
        "description": "Add a file to a dataset.",
        "options": {
            "survey_name": {
                "description": "The name of the dataset",
                "required": true
            },
            "data_type": {
                "description": "The type of data being added",
                "required": true
            },
            "path": {
                "description": "Path to the data (leave empty to use the current directory",
                "required": false
            }
        }
    },
    "clear": {
        "description": "Clear all data from a dataset",
        "options": {
            "survey_name": {
                "description": "The name of the dataset",
                "required": true
            }       
        }
    },
    "remove": {
        "description": "Remove a datatype from a dataset",
        "options": {
            "survey_name": {
                "description": "The name of the dataset",
                "required": true
            },
            "dtype": {
                "description": "The type of data being removed",
                "required": true
            }       
        }
    },
    "get": {
        "description": "Get the path to a specified data type in a dataset",
        "options": {
            "dataset_name": {
                "description": "The name of the datset",
                "required": true
            },
            "dtype": {
                "description": "The type of data being removed",
                "required": true
            }
        }
    }
}